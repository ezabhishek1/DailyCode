# Hint 

I saw most people using maxFreq and finding the solution but there is an another way to solve this problem.

i.e., checking for each upper case alphabet and finding the maxLen we can get by changing atmost k characters in a sliding window.

making its TC: O(n)

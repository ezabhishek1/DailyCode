# Predecessor and Successor

🔍 Intuition:

Since it's a BST, we can use its properties:

All nodes in the left subtree are smaller.

All nodes in the right subtree are larger.

This allows us to efficiently search for predecessor and successor in O(h) time, where h is the height of the BST.
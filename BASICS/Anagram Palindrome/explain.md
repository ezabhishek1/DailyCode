# idea

- Insted of using 26 size Array we can use a 32 bit integer as that similar array but will be using only last 26 bits so a index would be 0 then b would be 1, c index is 3 like this way.. and will check the whole string and substract 'a' from the current character to get the index so a - 'a' 'a' 'can become 0 index and will toggle that index and at last will count number of bits set as 1 so.. first will substract -bitmask - 1 and do bitwise and & operation becuse this operation decrement the bit count by 1 every time so.. at last would check that either bitmask is zero or not? Because if it were having only 1 bit set as 1 maxinum that it would become zero or otheriwse it would stay as zero so.. if Zero then it's palindrone otheriwse Not
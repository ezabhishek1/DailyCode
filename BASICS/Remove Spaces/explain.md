🚀 Optimal Approach for Removing Spaces (O(1) Space)

# Approach
Create an empty string called result.

Traverse every character of the given string.

Check if the current character is not a space ' '.

If it is not a space, add it to result.

After the loop ends, return result.

This is efficient because I only visit each character once.
use bareiss algorithm, instead of kirchoff. Although, time complexity is asymptotically similar, but because of 
direct integral values involvement, bareiss is efficient in memory management.

using fixed size array parsing and avoiding vector overhead, this is fastest possible code